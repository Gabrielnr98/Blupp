export interface WebsiteType {
    name: string;
    email: string;
}

export interface WebsiteSanitizerType {
    name: string;
    email: string;
}

export interface WebsiteReturnType {
    _id: string;
    name: string;
    email: string;
}
